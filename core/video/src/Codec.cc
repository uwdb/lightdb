#include "Codec.h"

namespace lightdb {

//    const Codec Codec::H264(std::string("H264"), NV_ENC_H264, cudaVideoCodec_H264);
  //  const Codec Codec::HEVC{"HEVC", NV_ENC_HEVC, cudaVideoCodec_HEVC};

} // namespace lightdb