language: cpp
dist: trusty
compiler:
  - gcc
os:
  - linux

addons:
  apt:
    sources:
      - sourceline: 'ppa:graphics-drivers/ppa'
#    packages: 
#      - nvidia-cuda-toolkit-8.0.44

script:
  - mkdir -p build
  - cd build
  - cmake ..
  - make
  - make test
  - ctest
